# Competition data: 751 MB and licensed CC BY-NC 4.0, so it is never
# redistributed here.  Fetch it with the kaggle CLI (see README).
data/

# Generated artefacts: caches, checkpoints, probability maps, logs.
artifacts/
*.pt
*.pth
*.npz
*.log

# Staging directories built by tools/sync_src.py and tools/push_train.py.
kernels/srcpkg/
kernels/cachepkg/
kernels/pulled/
kernels/_runs/
kernels/*/out/

# The host's self-evaluation notebook, pulled for reference.  It is their code,
# and this repository is MIT, so it is not redistributed here.  Our agreement
# with it is asserted by an independent reimplementation of its stated rules in
# tests/test_official_metric.py.
kernels/official/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# The host's report template and self-evaluation notebook, kept locally for
# reference. Both are theirs, not ours, so they are not redistributed here.
kaggle_comp_report_fi_segm_2026.pdf
self-evaluation-notebook.ipynb
